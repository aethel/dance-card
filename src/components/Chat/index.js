import React, { memo } from 'react'

export const ChatPage = memo(props => {
    // const fromID = sessionStorage.getItem('uid');
    console.log(props);
    
    return (
        <div>is a chat</div>
    )
})